# Assignment 3 – CI/CD with GitHub Actions

A Bash utility (`system-info`, `check-host`, `check-port`, `help`) with a local CI
pipeline: lint → tests → Docker build and smoke tests, mirrored by a GitHub Actions
workflow. Grade locally with `./grade.sh`. Documentation will be completed as the
pipeline is built.
